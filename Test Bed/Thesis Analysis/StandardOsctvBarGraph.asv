% **************************************
% Set Base Params
% **************************************
readPath = 'E:\Thesis Results\External Beam Trials.xls';
sheet = 'Stand. OSC-TV Param. Opt. ALYS';

groupLabels = {...
    'FDK',...
    '\begin{tabular}{c}OSC-TV\\S=[26..2]\end{tabular}',...
    '\begin{tabular}{c}OSC-TV\\S=[51..3]\end{tabular}',...
    '\begin{tabular}{c}OSC-TV\\S=[103..6]\end{tabular}',...
    '\begin{tabular}{c}OSC-TV\\S=[205..13]\end{tabular}',...
    '\begin{tabular}{c}OSC-TV\\S=[205..41]\end{tabular}'};

groupRows = {...
    3:3,...
    30:34,...
    35:39,...
    40:44,...
    45:49,...
    50:54};

subgroupLabels = {...
    'c=0.001',...
    'c=0.005',...
    'c=0.01',...
    'c=0.05',...
    'c=0.1'};

subgroupColours = [...
    0.8 0.8 0.8;...
    0.65 0.65 0.65;...
    0.50 0.50 0.50;...
    0.35 0.35 0.35;...
    0.20 0.20 0.20];

customColours = {...
    [0.05 0.05, 0.05],...
    [], [], [], [], []};

figDimsInCm = [10, 15];

% **************************************
% Mean
% **************************************

graphWritePath = 'E:\Thesis Results\Standard OSC-TV Bar Graphs\Mean Comparison (15 Iterations).png';

column = 10;

yAxisLabel = 'Change in $\bar{\mu}_{ROI}$ from control';

subgroupLabelWriteIndex = 2;
subgroupLabelOrientation = 'above';

title = 'Comparison of $\bar{\mu}_{ROI}$ for tested OSC-TV parameters';

barGraphDataCollectionAndCreation(...
    readPath, sheet, groupRows, column,...
    graphWritePath, groupLabels, subgroupLabels,...
    subgroupLabelWriteIndex, subgroupLabelOrientation,...
    figDimsInCm, subgroupColours, customColours,...
    yAxisLabel, title); 

% **************************************
% Variance
% **************************************

graphWritePath = 'E:\Thesis Results\Standard OSC-TV Bar Graphs\Variance Comparison (15 Iterations).png';

column = 13;

yAxisLabel = 'Change in $\sigma_{ROI}$ from control';

subgroupLabelWriteIndex = 2;
subgroupLabelOrientation = 'above-stepped';

title = 'Comparison of $\sigma_{ROI}$ for tested OSC-TV parameters';

barGraphDataCollectionAndCreation(...
    readPath, sheet, groupRows, column,...
    graphWritePath, groupLabels, subgroupLabels,...
    subgroupLabelWriteIndex, subgroupLabelOrientation,...
    figDimsInCm, subgroupColours, customColours,...
    yAxisLabel, title); 

% **************************************
% Gradient Mean
% **************************************

graphWritePath = 'E:\Thesis Results\Standard OSC-TV Bar Graphs\Gradient Comparison (15 Iterations).png';

column = 16;

yAxisLabel = 'Change in $\sigma_{ROI}$ from control';

subgroupLabelWriteIndex = 2;
subgroupLabelOrientation = 'above-stepped';

title = 'Comparison of $\sigma_{ROI}$ for tested OSC-TV parameters';

barGraphDataCollectionAndCreation(...
    readPath, sheet, groupRows, column,...
    graphWritePath, groupLabels, subgroupLabels,...
    subgroupLabelWriteIndex, subgroupLabelOrientation,...
    figDimsInCm, subgroupColours, customColours,...
    yAxisLabel, title); 