function bool = matricesEqual(mat1, mat2)
%bool = matricesEqual(mat1, mat2)

dims1 = size(mat1);
dims2 = size(mat2);

if length(dims1) == length(dims2) && all(dims1 == dims2)
    for i=1:le
else
    bool = false;
end


bool = length(dims1) == length(dims2) && all(dims1 == dims2) && all(mat1 == mat2);

end

